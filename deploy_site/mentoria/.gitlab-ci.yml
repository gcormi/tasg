pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r *.html *.png public/ 2>/dev/null || true
    - cp -r *.json public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
